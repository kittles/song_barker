welcome back-

server/app.js:
    - cloud to_crops endpoint needs to have real vals for
    the cloud server ip
    - cloud to_crops also needs to make entries in the db


cloud to_sequence implementation

do a little reading on the details of pods/instances/services/clusters/app and
the difference between them
